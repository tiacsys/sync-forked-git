# Copyright (c) 2021-2026 TiaC Systems
# SPDX-License-Identifier: Unlicense

name: Sync KiCad Project Forks

on:
  schedule: # At minute 50 past every 6th hour.
    - cron: '50 */6 * * *'
  workflow_dispatch: # And manually on button click.

jobs:
  libs-git-sync:
    name: Sync Libraries

    uses: tiacsys/github-workflows/.github/workflows/git-sync.yml@v2
    with:
      matrix: ./.github/git-sync-kicad.yaml
      secret-prefix: "SSH_PRIVATE_KEY_"
    secrets: inherit # pass all secrets
